name: Build&Push to GHCR

on:
    push:
        branches: [main]

jobs:
    build:
        runs-on: ubuntu-latest
        permissions:
            contents: read
            packages: write
        
        steps:
            - name: checkout
              uses: actions/checkout@v4

            - name: re-naming conventions
              run: echo "GHCR_OWNER=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_ENV

            - name: Log in to GHCR
              run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ env.GHCR_OWNER }} --password-stdin

            - name: "Build and push"
              run: |
                IMAGE_NAME=ghcr.io/${{ env.GHCR_OWNER }}/orphie
                docker build -t $IMAGE_NAME:latest .
                docker push $IMAGE_NAME:latest